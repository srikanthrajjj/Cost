name: Build and Deploy to Vercel

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Vercel
        uses: vercel/action@v6
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          # Vite bakes VITE_* at build time; also set the same key in the Vercel
          # project Environment Variables UI for Production (and Preview).
          build-env: VITE_SK_API_KEY=${{ secrets.VITE_SK_API_KEY }}
          env: VITE_SK_API_KEY=${{ secrets.VITE_SK_API_KEY }}
          production: true
          vercel-args: --force
